[build]
  command = "npm ci && npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--include=dev"
  PYTHON_VERSION = "3.11"

[[plugins]]
  package = "@netlify/plugin-nextjs"
